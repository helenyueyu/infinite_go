import { connect } from 'react-redux';

import TabsReputationIndex from './tabs_reputation_index'; 

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});


export default connect(mapStateToProps, mapDispatchToProps)(TabsReputationIndex); 