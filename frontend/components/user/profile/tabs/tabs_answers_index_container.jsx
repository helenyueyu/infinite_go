import { connect } from 'react-redux';

import TabsAnswersIndex from './tabs_answers_index'; 

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});


export default connect(mapStateToProps, mapDispatchToProps)(TabsAnswersIndex); 