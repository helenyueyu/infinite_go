import { connect } from 'react-redux';

import TabsSummaryIndex from './tabs_summary_index'; 

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});


export default connect(mapStateToProps, mapDispatchToProps)(TabsSummaryIndex); 