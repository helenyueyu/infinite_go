import { connect } from 'react-redux';

import TabsTagsIndex from './tabs_tags_index'; 

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});


export default connect(mapStateToProps, mapDispatchToProps)(TabsTagsIndex); 