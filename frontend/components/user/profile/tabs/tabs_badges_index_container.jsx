import { connect } from 'react-redux';

import TabsBadgesIndex from './tabs_badges_index'; 

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});


export default connect(mapStateToProps, mapDispatchToProps)(TabsBadgesIndex); 