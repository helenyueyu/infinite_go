import { connect } from 'react-redux';

import TabsQuestionsIndex from './tabs_questions_index'; 

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});


export default connect(mapStateToProps, mapDispatchToProps)(TabsQuestionsIndex); 