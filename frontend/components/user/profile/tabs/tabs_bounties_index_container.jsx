import { connect } from 'react-redux';

import TabsBountiesIndex from './tabs_bounties_index'; 

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});


export default connect(mapStateToProps, mapDispatchToProps)(TabsBountiesIndex); 